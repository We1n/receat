# 🚀 Пошаговая инструкция по деплою в продакшен

## 📋 Подготовка (локально на вашем компьютере)

### Шаг 1: Настройка Git (если еще не настроен)

1. **Откройте Git Bash** (или PowerShell/CMD)
2. **Проверьте настройки Git:**
   ```bash
   git config --global user.name "Ваше Имя"
   git config --global user.email "your.email@example.com"
   ```
3. **Проверьте подключение к репозиторию:**
   ```bash
   cd "D:\Vibe Coding\receat\receat"
   git remote -v
   ```
   Должен быть указан ваш GitHub репозиторий.

### Шаг 2: Сборка frontend (локально)

1. **Откройте PowerShell или CMD**
2. **Перейдите в папку frontend:**
   ```powershell
   cd "D:\Vibe Coding\receat\receat\eatsite\frontend"
   ```
3. **Установите зависимости (если еще не установлены):**
   ```powershell
   npm install
   ```
4. **Соберите проект для продакшена:**
   ```powershell
   npm run build
   ```
5. **Проверьте, что папка `dist` создалась:**
   ```powershell
   dir dist
   ```
   Должны быть файлы: `index.html`, `assets/`, `sw.js`, `manifest.webmanifest` и т.д.

---

## 🔌 Подключение к серверу через PuTTY

### Шаг 3: Настройка PuTTY

1. **Откройте PuTTY**
2. **Введите данные подключения:**
   - **Host Name (or IP address):** `155.212.165.45`
   - **Port:** `22`
   - **Connection type:** `SSH`
3. **В разделе "Connection" → "Data":**
   - **Auto-login username:** `webapp`
4. **В разделе "Session":**
   - **Saved Sessions:** введите `receat-server` и нажмите **Save**
5. **Нажмите "Open"** для подключения
6. **При первом подключении** появится предупреждение о ключе сервера - нажмите **"Yes"**
7. **Введите пароль** (если требуется)

---

## 📁 Копирование файлов через WinSCP

### Шаг 4: Настройка WinSCP

1. **Откройте WinSCP**
2. **Создайте новое подключение:**
   - **File protocol:** `SFTP`
   - **Host name:** `155.212.165.45`
   - **Port number:** `22`
   - **User name:** `webapp`
   - **Password:** (введите пароль)
3. **Нажмите "Save"** и сохраните как `receat-server`
4. **Нажмите "Login"**

### Шаг 5: Подготовка структуры на сервере

1. **В WinSCP перейдите в:** `/home/webapp/projects/`
2. **Проверьте, есть ли папка `eatsite`:**
   - Если нет - создайте её (правая кнопка → Create Directory → `eatsite`)
   - Если есть - откройте её

### Шаг 6: Копирование файлов eatsite

**⚠️ ВАЖНО:** Копируйте файлы в правильном порядке!

1. **Скопируйте backend (БЕЗ node_modules и data):**
   - В WinSCP слева (локальный компьютер) перейдите в: `D:\Vibe Coding\receat\receat\eatsite\backend`
   - Выделите все файлы и папки, **КРОМЕ:**
     - `node_modules/` (не копируйте!)
     - `data/` (не копируйте, если она уже есть на сервере!)
   - Перетащите в правую панель WinSCP: `/home/webapp/projects/eatsite/backend/`
   - Если папки `backend` нет на сервере - создайте её сначала

2. **Скопируйте frontend/dist:**
   - В WinSCP слева перейдите в: `D:\Vibe Coding\receat\receat\eatsite\frontend\dist`
   - Выделите **ВСЁ содержимое** папки `dist` (не саму папку!)
   - Перетащите в: `/home/webapp/projects/eatsite/frontend/dist/`
   - Если папки `frontend/dist` нет - создайте её сначала

3. **Скопируйте корневые файлы eatsite:**
   - Скопируйте следующие файлы из `D:\Vibe Coding\receat\receat\eatsite\`:
     - `server.js`
     - `ecosystem.config.js`
     - `update.sh`
     - `ENV_EXAMPLE.txt` (для справки)
   - В папку: `/home/webapp/projects/eatsite/`

4. **Скопируйте shared (если есть):**
   - Скопируйте папку `shared/` из `eatsite/shared/` в `/home/webapp/projects/eatsite/shared/`

---

## ⚙️ Настройка на сервере (через PuTTY)

### Шаг 7: Подключение через PuTTY

1. **Откройте PuTTY** и подключитесь к серверу (используйте сохраненную сессию `receat-server`)
2. **Перейдите в директорию проекта:**
   ```bash
   cd /home/webapp/projects/eatsite
   ```

### Шаг 8: Установка зависимостей backend

1. **Перейдите в папку backend:**
   ```bash
   cd backend
   ```
2. **Установите зависимости:**
   ```bash
   npm install --production
   ```
   Это может занять несколько минут.

3. **Вернитесь в корень проекта:**
   ```bash
   cd ..
   ```

### Шаг 9: Создание папок и файлов

1. **Создайте необходимые папки:**
   ```bash
   mkdir -p logs backend/data
   ```

2. **Создайте файл .env:**
   ```bash
   nano .env
   ```
   
3. **Вставьте следующее содержимое:**
   ```
   PORT=8082
   BACKEND_PORT=3000
   BACKEND_HOST=localhost
   MAX_CLIENTS_PER_WORKSPACE=5
   NODE_ENV=production
   ```
   
4. **Сохраните файл:**
   - Нажмите `Ctrl + O` (сохранить)
   - Нажмите `Enter` (подтвердить имя файла)
   - Нажмите `Ctrl + X` (выйти)

### Шаг 10: Настройка прав доступа

1. **Сделайте скрипт update.sh исполняемым:**
   ```bash
   chmod +x update.sh
   ```

---

## 🚀 Запуск проекта через PM2

### Шаг 11: Запуск процессов

1. **Запустите проект через PM2:**
   ```bash
   pm2 start ecosystem.config.js
   ```

2. **Проверьте статус:**
   ```bash
   pm2 list
   ```
   Должны быть запущены:
   - `eatsite-backend` (статус: online)
   - `eatsite-frontend` (статус: online)

3. **Сохраните конфигурацию PM2:**
   ```bash
   pm2 save
   ```
   Это нужно, чтобы процессы автоматически запускались после перезагрузки сервера.

4. **Проверьте логи:**
   ```bash
   pm2 logs eatsite-backend --lines 20
   pm2 logs eatsite-frontend --lines 20
   ```
   Не должно быть ошибок (красных сообщений).

---

## ✅ Проверка работы

### Шаг 12: Проверка eatsite

1. **Откройте в браузере:**
   ```
   http://155.212.165.45:8082
   ```
   Должен открыться интерфейс приложения.

2. **Проверьте API:**
   ```
   http://155.212.165.45:3000/health
   ```
   Должен вернуться JSON с `{"status": "ok"}` или подобное.

---

## 🔄 Обновление проекта (после изменений)

### Вариант 1: Через Git (рекомендуется)

1. **Локально:**
   ```powershell
   cd "D:\Vibe Coding\receat\receat"
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

2. **На сервере (через PuTTY):**
   ```bash
   cd /home/webapp/projects/eatsite
   ./update.sh
   ```

3. **Для frontend (если изменился):**
   - Локально соберите: `cd eatsite/frontend && npm run build`
   - Скопируйте `frontend/dist` через WinSCP на сервер
   - Перезапустите: `pm2 restart eatsite-frontend`

### Вариант 2: Вручную через WinSCP

1. **Соберите frontend локально** (если изменился)
2. **Скопируйте измененные файлы** через WinSCP
3. **На сервере перезапустите:**
   ```bash
   pm2 restart all
   ```

---

## 🛠️ Полезные команды PM2

```bash
# Просмотр всех процессов
pm2 list

# Просмотр логов
pm2 logs eatsite-backend
pm2 logs eatsite-frontend

# Перезапуск
pm2 restart eatsite-backend
pm2 restart eatsite-frontend
pm2 restart all

# Остановка
pm2 stop eatsite-backend

# Удаление из PM2
pm2 delete eatsite-backend

# Мониторинг
pm2 monit
```

---

## 🐛 Решение проблем

### Проблема: "Port already in use"

```bash
# Проверьте, что использует порт
sudo lsof -i :8082
sudo lsof -i :3000

# Остановите процесс или измените порт в ecosystem.config.js
```

### Проблема: "Cannot find module"

```bash
# Переустановите зависимости
cd /home/webapp/projects/eatsite/backend
rm -rf node_modules
npm install --production
```

### Проблема: "Permission denied"

```bash
# Проверьте права доступа
ls -la /home/webapp/projects/eatsite

# Дайте права на выполнение
chmod +x update.sh
chmod +x server.js
```

### Проблема: Frontend не открывается

1. Проверьте, что `frontend/dist` скопирован правильно
2. Проверьте логи: `pm2 logs eatsite-frontend`
3. Проверьте, что `index.html` существует в `frontend/dist/`

---

## 📝 Чек-лист деплоя

- [ ] Git настроен локально
- [ ] Frontend собран локально (`npm run build`)
- [ ] PuTTY настроен и подключение работает
- [ ] WinSCP настроен и подключение работает
- [ ] Backend скопирован на сервер (без node_modules)
- [ ] Frontend/dist скопирован на сервер
- [ ] Корневые файлы скопированы (server.js, ecosystem.config.js, update.sh)
- [ ] Зависимости backend установлены (`npm install --production`)
- [ ] Файл .env создан и настроен
- [ ] Папки logs и backend/data созданы
- [ ] PM2 процессы запущены и работают
- [ ] PM2 конфигурация сохранена (`pm2 save`)
- [ ] Сайт открывается в браузере
- [ ] API отвечает на запросы

---

## 📞 Контакты и поддержка

Если возникли проблемы:
- Проверьте логи: `pm2 logs`
- Проверьте статус: `pm2 list`
- Убедитесь, что все файлы скопированы правильно
- Проверьте права доступа к файлам

**Удачного деплоя! 🚀**

