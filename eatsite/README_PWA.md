# 🚀 PWA настройка для Eatsite - Краткая инструкция

## ✅ Что было сделано

1. ✅ Создан production сервер (`server.js`) на порту **8082**
2. ✅ Настроен vite-plugin-pwa с Service Worker
3. ✅ Обновлён manifest.json с правильными путями
4. ✅ Создан скрипт для копирования PWA файлов
5. ✅ Добавлены команды сборки в package.json

## 📋 Что нужно сделать

### 1. Установить зависимости (если еще не установлены)

```bash
cd frontend
npm install
```

### 2. Собрать frontend

```bash
cd frontend
npm run build
```

### 3. Запустить backend (в отдельном терминале)

```bash
cd backend
npm start
```

### 4. Запустить production сервер

```bash
# Из корня проекта eatsite
node server.js
```

Приложение будет доступно на **http://localhost:8082**

## 🔧 Настройка на сервере

### Порт: **8082**

Этот порт выбран чтобы не конфликтовать с CRM App (порт 8081).

### Запуск через PM2

```bash
cd /path/to/eatsite
pm2 start server.js --name eatsite
pm2 save
```

### Проверка

```bash
pm2 list
pm2 logs eatsite
```

## 📁 Структура файлов

```
eatsite/
├── server.js              ← Production сервер (НОВЫЙ)
├── scripts/
│   └── copy-pwa-files.js  ← Скрипт копирования PWA файлов (НОВЫЙ)
├── frontend/
│   ├── dist/              ← Собранные файлы (создаётся после npm run build)
│   ├── vite.config.js     ← Обновлён с PWA настройками
│   └── manifest.json      ← Обновлён с правильными путями
└── PWA_SETUP.md          ← Подробная инструкция (НОВЫЙ)
```

## ⚙️ Переменные окружения

```bash
# Изменить порт production сервера
PORT=8082 node server.js

# Изменить порт backend
BACKEND_PORT=3000 node server.js
```

## 🎯 Что дальше?

1. Скопируйте папку `eatsite` в отдельный проект
2. Удалите папку `eatsite` из текущего проекта
3. Следуйте инструкциям выше для запуска

Подробная документация: см. `PWA_SETUP.md`

