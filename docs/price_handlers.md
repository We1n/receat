# 📊 Обработчики цен

## 📋 Описание
Модуль содержит обработчики для работы с ценами продуктов в боте.

## 🎯 Функциональность
- Проверка цен продуктов
- Добавление новых цен
- Обновление существующих цен
- Удаление цен
- Поиск цен по названию

## 🔄 Состояния FSM
- `WAITING_PRICE`: Ожидание ввода цены от пользователя

## 🛠️ Обработчики

### 1. Проверка цены
```python
async def handle_price_check(update: Update, context: ContextTypes.DEFAULT_TYPE)
```
Проверяет цену продукта и отправляет информацию пользователю.

**Параметры:**
- `update`: Обновление от Telegram
- `context`: Контекст обработчика

**Возвращает:**
- `None`

### 2. Добавление цены
```python
async def handle_price_add(update: Update, context: ContextTypes.DEFAULT_TYPE)
```
Запрашивает у пользователя цену для нового продукта.

**Параметры:**
- `update`: Обновление от Telegram
- `context`: Контекст обработчика

**Возвращает:**
- `WAITING_PRICE`: Состояние ожидания ввода цены

### 3. Сохранение цены
```python
async def handle_price_save(update: Update, context: ContextTypes.DEFAULT_TYPE)
```
Сохраняет введенную пользователем цену.

**Параметры:**
- `update`: Обновление от Telegram
- `context`: Контекст обработчика

**Возвращает:**
- `-1`: Завершение диалога
- `WAITING_PRICE`: Повторный запрос при неверном формате

### 4. Удаление цены
```python
async def handle_price_delete(update: Update, context: ContextTypes.DEFAULT_TYPE)
```
Удаляет цену продукта.

**Параметры:**
- `update`: Обновление от Telegram
- `context`: Контекст обработчика

**Возвращает:**
- `None`

### 5. Обновление цены
```python
async def handle_price_update(update: Update, context: ContextTypes.DEFAULT_TYPE)
```
Запрашивает у пользователя новую цену для продукта.

**Параметры:**
- `update`: Обновление от Telegram
- `context`: Контекст обработчика

**Возвращает:**
- `WAITING_PRICE`: Состояние ожидания ввода цены

### 6. Поиск цен
```python
async def handle_price_search(update: Update, context: ContextTypes.DEFAULT_TYPE)
```
Ищет цены по названию продукта.

**Параметры:**
- `update`: Обновление от Telegram
- `context`: Контекст обработчика

**Возвращает:**
- `None`

## 📝 Примеры использования

### Проверка цены
```python
# Пользователь отправляет: /check_price молоко
await handle_price_check(update, context)
# Бот отвечает: "Цена молока: 89.9 руб. (обновлено 01.01.2024)"
```

### Добавление цены
```python
# Пользователь нажимает: "Добавить"
await handle_price_add(update, context)
# Бот отвечает: "Введите цену для молока:"
```

### Сохранение цены
```python
# Пользователь отправляет: "89.9"
await handle_price_save(update, context)
# Бот отвечает: "Цена молока успешно сохранена!"
```

## 🔍 Обработка ошибок
- Неверный формат цены
- Продукт не найден
- Ошибки при сохранении/удалении
- Ошибки при поиске

## 📊 Метрики
- Количество проверок цен
- Количество добавлений цен
- Количество обновлений цен
- Количество удалений цен
- Количество поисков цен
- Время выполнения операций
- Количество ошибок

## 🔒 Безопасность
- Валидация входных данных
- Проверка прав доступа
- Логирование действий
- Защита от инъекций

## ⚡ Производительность
- Кэширование цен
- Оптимизация поиска
- Асинхронные операции
- Эффективное хранение данных

## 📈 Мониторинг
- Логирование действий
- Отслеживание ошибок
- Сбор метрик
- Анализ использования

## 🧪 Тесты
- Модульные тесты
- Интеграционные тесты
- Тесты производительности
- Тесты безопасности 